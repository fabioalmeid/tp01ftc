// This JLex file was machine-generated by the BNF converter
package monitor;

import java_cup.runtime.*;
%%
%cup
%unicode
%line
%public
%{
  String pstring = new String();
  public int line_num() { return (yyline+1); }
  public String buff() { return new String(yy_buffer,yy_buffer_index,10).trim(); }
%}
LETTER = ({CAPITAL}|{SMALL})
CAPITAL = [A-Z\xC0-\xD6\xD8-\xDE]
SMALL = [a-z\xDF-\xF6\xF8-\xFF]
DIGIT = [0-9]
IDENT = ({LETTER}|{DIGIT}|['_])
%state COMMENT
%state CHAR
%state CHARESC
%state CHAREND
%state STRING
%state ESCAPED
%%
<YYINITIAL>Temperatura\ de\ { return new Symbol(sym._SYMB_0); }
<YYINITIAL>g/dL { return new Symbol(sym._SYMB_1); }
<YYINITIAL>mg/dL { return new Symbol(sym._SYMB_2); }
<YYINITIAL>Pressao\ Arterial { return new Symbol(sym._SYMB_3); }
<YYINITIAL>: { return new Symbol(sym._SYMB_4); }
<YYINITIAL>h: { return new Symbol(sym._SYMB_5); }
<YYINITIAL>Bilirrubina { return new Symbol(sym._SYMB_6); }
<YYINITIAL>C { return new Symbol(sym._SYMB_7); }
<YYINITIAL>Hemoglobina { return new Symbol(sym._SYMB_8); }
<YYINITIAL>as { return new Symbol(sym._SYMB_9); }
<YYINITIAL>e { return new Symbol(sym._SYMB_10); }
<YYINITIAL>m { return new Symbol(sym._SYMB_11); }
<YYINITIAL>mmHg { return new Symbol(sym._SYMB_12); }





<YYINITIAL>{DIGIT}+ { return new Symbol(sym._INTEGER_, new Integer(yytext())); }

<YYINITIAL>[ \t\r\n\f] { /* ignore white space. */ }
